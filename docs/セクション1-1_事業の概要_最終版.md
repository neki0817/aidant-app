# セクション1-1: 事業の概要（最終版）

## 質問順序の改善ポイント

### 修正1: 開業日を前に移動
- サイトから取得できない基本情報は先に聞く
- 法人の設立日と店舗の開業日を分けて質問

### 修正2: 業種は自動判別
- Google Mapsの`types`情報から自動判別
- ユーザー確認のみ（修正可能）

---

## 最終的な質問順序

1. **Q1-6（法人/個人事業主）**
2. **Q1-2（法人名と店舗名の違い）**（条件付き）
3. **Q1-2-company（法人名）**（条件付き）
4. **Q1-5-corporate（法人設立日）**（条件付き）← 新規追加
5. **Q1-5（店舗開業日）**← 前に移動
6. **Q1-0（Google Maps検索）**
7. **Q1-0-analysis（自動分析）**
8. **Q1-0-website（追加URL）**
9. **Q1-0-profile（プロフィール確認）**
10. **Q1-1（業種確認）**← 自動判別＋確認
11. **Q1-3（商品・サービス）**
12. **Q1-NEW-1（コンセプト）**
13. **Q1-NEW-2（経営者の経歴）**
14. **Q1-NEW-3（こだわり）**
15. **Q1-NEW-4（顧客層）**
16. **Q1-NEW-5（利用パターン）**

---

## 詳細な質問設計

### ❶ Q1-6: 法人/個人事業主
**優先度:** 1（最優先）

**質問文:**
```
まず、事業形態について教えてください

1. 法人（株式会社、合同会社など）
2. 個人事業主

💡 番号を入力してください（例: 1）
```

**入力タイプ:** single_select

**選択肢:**
- `corporate`: 「法人（株式会社、合同会社など）」
- `individual`: 「個人事業主」

**dependencies:** なし

**様式2への反映:**
- 申請者欄の「法人/個人」区分

---

### ❷ Q1-2: 法人名と店舗名の違い
**優先度:** 2

**条件:** Q1-6で「法人」を選択した場合のみ表示

**質問文:**
```
法人名（会社名）と店舗名は同じですか？

1. 同じです（例：株式会社〇〇 = 店舗名〇〇）
2. 異なります（例：株式会社△△が「〇〇」という店舗を運営）

💡 番号を入力してください（例: 2）

例：株式会社クレアバッカスが「トラットリア・ベッラ」という店舗を運営している場合は「2」を選択
```

**入力タイプ:** single_select

**選択肢:**
- `same`: 「同じです（法人名 = 店舗名）」
- `different`: 「異なります（法人名があります）」

**dependencies:** `['Q1-6']`

**condition:**
```javascript
(answers) => answers['Q1-6'] === 'corporate'
```

**様式2への反映:**
- 申請者名と事業所名の区別

---

### ❸ Q1-2-company: 法人名
**優先度:** 3

**条件:** Q1-2で「異なります」を選択した場合のみ表示

**質問文:**
```
法人名（会社名）を教えてください

例：株式会社〇〇、合同会社△△
```

**入力タイプ:** text

**placeholder:** 「例：株式会社クレアバッカス」

**dependencies:** `['Q1-2']`

**condition:**
```javascript
(answers) => answers['Q1-2'] === 'different'
```

**様式2への反映:**
- 「申請者名」欄に記載

---

### ❹ Q1-5-corporate: 法人設立日【新規追加】
**優先度:** 4

**条件:** Q1-6で「法人」を選択した場合のみ表示

**質問文:**
```
法人の設立日を教えてください

例: 2018/4/1
```

**入力タイプ:** date

**placeholder:** 「例：2018/04/01」

**dependencies:** `['Q1-6']`

**condition:**
```javascript
(answers) => answers['Q1-6'] === 'corporate'
```

**required:** true

**helpText:**
```
💡 登記簿に記載されている会社の設立年月日を入力してください
```

**様式2への反映:**
- 法人の場合の設立年月日

---

### ❺ Q1-5: 店舗開業日
**優先度:** 5（前に移動）

**質問文（動的生成）:**
```javascript
(answers) => {
  const isCorporate = answers['Q1-6'] === 'corporate';
  const isDifferent = answers['Q1-2'] === 'different';

  if (isCorporate && isDifferent) {
    return '店舗の開業日を教えてください\n\n例: 2019/5/1';
  } else if (isCorporate && !isDifferent) {
    return '事業開始日を教えてください\n\n例: 2019/5/1\n\n※法人設立と同時に事業を開始した場合は、設立日と同じ日付を入力してください';
  } else {
    return '開業日を教えてください\n\n例: 2019/5/1';
  }
}
```

**入力タイプ:** date

**placeholder:** 「例：2019/05/01」

**dependencies:** `['Q1-6']`

**required:** true

**helpText（動的生成）:**
```javascript
(answers) => {
  const isCorporate = answers['Q1-6'] === 'corporate';
  const isDifferent = answers['Q1-2'] === 'different';

  if (isCorporate && isDifferent) {
    return '💡 この店舗を開業した日付を入力してください（法人設立日とは異なります）';
  } else if (isCorporate && !isDifferent) {
    return '💡 事業を開始した日付を入力してください';
  } else {
    return '💡 開業届に記載した開業日を入力してください';
  }
}
```

**様式2への反映:**
- 「2019年5月に東京都世田谷区三軒茶屋にて開業したイタリアンレストランです」

**自動入力のデフォルト値:**
```javascript
// Q1-5-corporateと同じ値を初期表示（修正可能）
if (answers['Q1-5-corporate'] && !isDifferent) {
  defaultValue = answers['Q1-5-corporate'];
}
```

---

### ❻ Q1-0: Google Maps検索
**優先度:** 6（開業日確認後に移動）

**質問文（動的生成）:**
```javascript
(answers) => {
  const isCorporate = answers['Q1-6'] === 'corporate';
  const isDifferent = answers['Q1-2'] === 'different';

  let searchTarget = '';
  if (isCorporate && isDifferent) {
    searchTarget = '店舗名';
  } else if (isCorporate && !isDifferent) {
    searchTarget = '法人名（店舗名）';
  } else {
    searchTarget = '店舗名・屋号';
  }

  return `${searchTarget}を教えてください\n\n店舗名や住所を入力いただければ、Google Mapsから営業時間や口コミ情報を自動で取得します📍`;
}
```

**入力タイプ:** text

**placeholder:** 「例：トラットリア・ベッラ 三軒茶屋」

**dependencies:** `['Q1-5']`（開業日の後）

**required:** true

**取得情報:**
- 店舗名・会社名
- 所在地・住所
- 営業時間・定休日
- 口コミ評価・件数
- **業種（types）** ← 業種判別に使用
- 電話番号
- ウェブサイトURL

**Google Maps typesの例:**
```javascript
// 飲食店の場合
types: ["restaurant", "food", "point_of_interest", "establishment"]

// 美容室の場合
types: ["hair_care", "beauty_salon", "point_of_interest", "establishment"]

// 小売店の場合
types: ["store", "clothing_store", "point_of_interest", "establishment"]
```

**様式2への反映:**
- 「東京都世田谷区三軒茶屋にて開業」
- 「Google Mapsの口コミ評価は4.3（120件）」

---

### ❼ Q1-0-analysis: Google Maps情報の分析
**優先度:** 7（自動実行）

**表示内容:**
```
✅ 【{店舗名}】の情報を取得しました！

📍 所在地: {住所}
⭐ Google口コミ: {評価} ({件数}件)
📅 営業日: {営業時間概要}
🌐 ウェブサイト: {URL}
🏷️ 業種: {自動判別した業種}
```

**入力タイプ:** ai_place_analysis

**dependencies:** `['Q1-0']`

**autoProgress:** true（ユーザー入力不要で自動進行）

**業種の自動判別ロジック:**
```javascript
// Google Maps typesから業種を判別
const determineBusinessType = (types) => {
  const typeMap = {
    // 飲食
    'restaurant': 'レストラン',
    'cafe': 'カフェ',
    'bar': 'バー',
    'bakery': 'ベーカリー',
    'meal_takeaway': 'テイクアウト',

    // 美容
    'hair_care': '美容室',
    'beauty_salon': '美容サロン',
    'spa': 'エステ・スパ',
    'nail_salon': 'ネイルサロン',

    // 小売
    'clothing_store': 'アパレル',
    'shoe_store': '靴店',
    'book_store': '書店',
    'jewelry_store': '宝飾店',
    'furniture_store': '家具店',
    'electronics_store': '家電店',
    'florist': '花屋',
    'store': '小売店',

    // サービス
    'gym': 'フィットネス',
    'laundry': 'クリーニング',
    'car_repair': '自動車修理',
    'dentist': '歯科',
    'veterinary_care': '動物病院',
    'locksmith': '鍵屋',

    // その他
    'point_of_interest': '店舗・施設'
  };

  // 優先順位順にマッチング
  for (const type of types) {
    if (typeMap[type]) {
      return typeMap[type];
    }
  }

  return null; // 判別できない場合
};
```

**様式2への反映:**
- 「Google Mapsの口コミ評価は4.3（120件）と、近隣の競合店（平均3.8）と比較して高い評価をいただいています」

---

### ❽ Q1-0-website: 追加Webサイト情報
**優先度:** 8

**質問文（動的生成）:**
```javascript
(answers) => {
  const placeInfo = answers['Q1-0'];
  const hasWebsite = placeInfo && placeInfo.website;

  if (hasWebsite) {
    return `公式サイト（${placeInfo.website}）を取得しました。\n\n食べログ・ホットペッパーなど、他のWebページのURLがあれば追加してください（任意）\n\nより詳細な顧客の声を取得できます📍`;
  } else {
    return 'お店や会社のWebページのURLがあれば教えてください（任意）\n\nホームページの情報から、より精度の高い質問を自動生成できます📍';
  }
}
```

**入力タイプ:** text

**placeholder:** 「例：https://tabelog.com/〇〇、https://beauty.hotpepper.jp/〇〇」

**dependencies:** `['Q1-0', 'Q1-0-analysis']`

**required:** false（任意）

**helpText（動的生成）:**
```javascript
(answers) => {
  const placeInfo = answers['Q1-0'];
  const hasWebsite = placeInfo && placeInfo.website;

  const baseHelp = '💡 以下のようなURLが利用できます：\n' +
    '【飲食店】食べログ、ぐるなび、ホットペッパーグルメ、Instagram\n' +
    '【美容室】ホットペッパービューティー、楽天ビューティー、Instagram\n' +
    '【小売・サービス】Instagram、Facebook、ECサイト\n\n';

  if (hasWebsite) {
    return baseHelp + '💡 公式サイトに加えて口コミサイトのURLを追加すると、顧客の生の声をより詳しく分析できます。\n\n入力しなくても質問は続けられます。';
  } else {
    return baseHelp + '入力しなくても質問は続けられます。';
  }
}
```

**様式2への反映:**
- Webサイトから取得した「顧客の声」「商品情報」を企業概要に反映

---

### ❾ Q1-0-profile: 店舗プロフィール確認
**優先度:** 9

**質問文:**
```
以下の店舗プロフィールをご確認ください。

修正が必要な項目はクリックして編集できます。
```

**入力タイプ:** store_profile

**dependencies:** `['Q1-0', 'Q1-0-analysis']`

**helpText:**
```
💡 Google MapsとWebサイトから自動で作成しました。間違いがあれば修正してください。
```

**生成される情報:**
- businessName（事業名）
- **businessType（業種）** ← Q1-0-analysisで判別した業種を含む
- corporateType（法人/個人）
- companyName（法人名）
- products（商品・サービス）
- strengths（強み）
- targetCustomers（ターゲット顧客）
- operatingDays（営業日）
- estimatedPrice（推定客単価）
- rating（評価）
- reviewCount（口コミ件数）
- reviewHighlights（口コミハイライト）

**様式2への反映:**
- 以降の質問で自動入力される情報の基盤となる

---

### ❿ Q1-1: 業種確認（自動判別＋確認）
**優先度:** 10

**質問文（動的生成）:**
```javascript
(answers) => {
  const profile = answers['Q1-0-profile'];
  const autoDetectedType = profile?.businessType;

  if (autoDetectedType) {
    return `業種は「${autoDetectedType}」で合っていますか？\n\n修正が必要な場合は、正しい業種を入力してください。\nそのままで良い場合は「次へ」を押してください。`;
  } else {
    return 'どんな業種ですか？\n\n例えば、「レストラン」「美容室」「雑貨店」のように教えてください';
  }
}
```

**入力タイプ:** text

**placeholder（動的生成）:**
```javascript
(answers) => {
  const profile = answers['Q1-0-profile'];
  const autoDetectedType = profile?.businessType;

  return autoDetectedType || '例：イタリアンレストラン、美容室、アパレルショップ';
}
```

**dependencies:** `['Q1-0', 'Q1-0-profile']`

**required:** true

**aiEnhance:** true

**helpText:**
```
💡 Google Mapsの情報から自動判別しました。具体的な業種名で構いません。
```

**自動入力（初期値として設定）:**
```javascript
// Q1-0-profileから自動判別した業種を初期値として設定
if (profile?.businessType) {
  defaultValue = profile.businessType;
}
```

**ユーザー操作:**
1. 自動判別が正しい場合 → そのまま「次へ」
2. 修正が必要な場合 → テキストを編集して送信

**様式2への反映:**
- 「イタリアンレストランです」

---

### ⓫ Q1-3: 商品・サービス
**優先度:** 11

**質問文:**
```
どんな商品・サービスを提供していますか？
```

**入力タイプ:** text

**placeholder（動的生成）:**
```javascript
(answers) => {
  const profile = answers['Q1-0-profile'];
  if (profile?.products && profile.products.length > 0) {
    return profile.products.join('、');
  }
  return '例：イタリア料理、ワイン販売';
}
```

**dependencies:** `['Q1-1']`

**required:** true

**aiEnhance:** false

**helpText:**
```
💡 Google Mapsの情報を参考に入力してください
```

**dynamicPlaceholder:** true（Google Maps情報から自動提案）

**自動入力（初期値として設定）:**
```javascript
// Q1-0-profileから自動入力
if (profile?.products && profile.products.length > 0) {
  defaultValue = profile.products.join('、');
}
```

**様式2への反映:**
- 「本格的なイタリア料理を手頃な価格で提供」

---

### ⓬ Q1-NEW-1: 事業のコンセプト・特徴【新規】
**優先度:** 12

**質問文:**
```
あなたの事業のコンセプトや特徴を教えてください

例:
- 「本格的なイタリア料理を手頃な価格で提供」
- 「地域密着型の美容室」
- 「オーガニック食材にこだわった健康志向のカフェ」
```

**入力タイプ:** textarea

**placeholder:**
```
例：本格的なイタリア料理を手頃な価格で提供することをコンセプトに、地域のお客様を中心に営業しています
```

**dependencies:** `['Q1-3']`

**required:** true

**maxLength:** 200文字

**helpText:**
```
💡 あなたの事業が「何を大切にしているか」「どんな価値を提供しているか」を自由に書いてください
```

**様式2への反映:**
- 「本格的なイタリア料理を手頃な価格で提供することをコンセプトに、地域のお客様を中心に営業しています」

---

### ⓭ Q1-NEW-2: 経営者の経歴・専門性【新規】
**優先度:** 13

**質問文:**
```
あなた（経営者）の経歴や専門性を教えてください

例:
- 「イタリア・トスカーナ地方で5年間修行、料理長経験あり」
- 「美容師歴15年、カラーリストとして受賞歴あり」
- 「大手アパレルでバイヤー経験10年」

特にない場合は「なし」と入力してください
```

**入力タイプ:** textarea

**placeholder:**
```
例：イタリア・トスカーナ地方で5年間修行し、現地のリストランテで料理長を務めた経験があります
```

**dependencies:** `['Q1-NEW-1']`

**required:** true

**maxLength:** 300文字

**helpText:**
```
💡 資格、修行経験、受賞歴、前職の経験など、あなたの専門性をアピールできる内容を書いてください
```

**様式2への反映:**
- 「オーナーシェフである私がイタリア・トスカーナ地方で5年間修行し、現地のリストランテで料理長を務めた経験を活かし、本場の味を再現しています」

---

### ⓮ Q1-NEW-3: こだわりポイント【新規】
**優先度:** 14

**質問文:**
```
商品・サービスの「こだわり」を教えてください

例:
- 「食材はイタリアの生産者から直輸入」
- 「オーガニック野菜のみを使用」
- 「国家資格保有者のみが施術」
- 「すべてハンドメイド」

特にない場合は「なし」と入力してください
```

**入力タイプ:** textarea

**placeholder:**
```
例：食材は信頼できるイタリアの生産者から直輸入しており、他店では味わえない本格的な料理を提供しています
```

**dependencies:** `['Q1-NEW-2']`

**required:** true

**maxLength:** 200文字

**helpText:**
```
💡 原材料、製法、技術、設備など、品質を支える「こだわり」を具体的に書いてください
```

**様式2への反映:**
- 「食材は信頼できるイタリアの生産者から直輸入しており、他店では味わえない本格的な料理を提供している点が特徴です」

---

### ⓯ Q1-NEW-4: ターゲット顧客層【新規】
**優先度:** 15

**質問文:**
```
主な顧客層を教えてください

例:
- 「30代〜50代の女性が中心」
- 「ファミリー層」
- 「ビジネスパーソン」
- 「高齢者」

複数ある場合は、「30代女性、ファミリー層」のように入力してください
```

**入力タイプ:** text

**placeholder（動的生成）:**
```javascript
(answers) => {
  const profile = answers['Q1-0-profile'];
  if (profile?.targetCustomers && profile.targetCustomers.length > 0) {
    return `参考：${profile.targetCustomers.join('、')}`;
  }
  return '例：30代〜50代の女性、ファミリー層';
}
```

**dependencies:** `['Q1-NEW-3']`

**required:** true

**maxLength:** 100文字

**helpText:**
```
💡 年齢層、性別、職業、ライフスタイルなど、主なお客様の特徴を教えてください
```

**自動入力（参考情報）:**
```javascript
// Q1-0-profileから参考情報を取得してplaceholderに表示
if (profile?.targetCustomers && profile.targetCustomers.length > 0) {
  dynamicPlaceholder = `参考：${profile.targetCustomers.join('、')}`;
}
```

**様式2への反映:**
- 「主な顧客層は30代〜50代の女性（約6割）、ファミリー層（約3割）で」

---

### ⓰ Q1-NEW-5: 顧客の利用パターン【新規】
**優先度:** 16

**質問文:**
```
お客様はどのような時に利用されますか？

例:
- 「平日はランチ、週末はディナー」
- 「月に1回の定期メンテナンス」
- 「季節ごとのイベント」
- 「通勤・通学の帰りに立ち寄り」

わからない場合は「わからない」と入力してください
```

**入力タイプ:** text

**placeholder:** 「例：平日はランチ利用、週末はディナーでのファミリー利用が中心」

**dependencies:** `['Q1-NEW-4']`

**required:** true

**maxLength:** 150文字

**helpText:**
```
💡 お客様がどのようなタイミング・目的で来店・利用されるかを教えてください
```

**様式2への反映:**
- 「平日はランチ利用、週末はディナーでのファミリー利用が中心となっています」

---

## セクション1-1完了メッセージ

**表示内容:**
```
✅ 事業の概要セクションが完了しました！

次は「売上高・利益の推移」について詳しくお伺いします。
```

---

## まとめ

### セクション1-1の質問数
- **既存質問:** 8問（Q1-6, Q1-2, Q1-2-company, Q1-5-corporate, Q1-5, Q1-0, Q1-0-analysis, Q1-0-website, Q1-0-profile, Q1-1, Q1-3）
- **新規質問:** 6問（Q1-5-corporate, Q1-NEW-1〜Q1-NEW-5）
- **合計:** 16問

### 自動判別・自動入力される質問
- **Q1-1（業種）** ← Google Maps typesから自動判別 → ユーザー確認
- **Q1-3（商品・サービス）** ← Q1-0-profileから初期値設定 → ユーザー確認

### 条件付き質問
- **Q1-2（法人名の違い）** ← Q1-6が「法人」の場合のみ
- **Q1-2-company（法人名）** ← Q1-2が「異なります」の場合のみ
- **Q1-5-corporate（法人設立日）** ← Q1-6が「法人」の場合のみ

### 質問順序の最適化ポイント

1. **基本情報を先に確認**
   - 法人/個人 → 法人名 → 設立日・開業日 → Google Maps検索

2. **サイトから取得できない情報は先に聞く**
   - 開業日、設立日は先に確認

3. **自動判別できる情報は確認のみ**
   - 業種はGoogle Maps typesから自動判別
   - ユーザーは修正が必要な場合のみ編集

4. **コンテキストの流れを重視**
   - 基本情報 → 事業内容 → 特徴・強み → 顧客層

### 様式2「事業の概要」への反映度
**95%完成**（残り5%はPhase 2-3の詳細情報で補完）

---

## 実装時の考慮点

### Google Maps typesからの業種判別
```javascript
// functions/index.js の generateStoreProfile
const determineBusinessType = (types) => {
  // 優先順位の高い順にマッチング
  const typeMap = {
    'restaurant': 'レストラン',
    'cafe': 'カフェ',
    'bar': 'バー',
    'hair_care': '美容室',
    'beauty_salon': '美容サロン',
    'clothing_store': 'アパレル',
    // ...
  };

  for (const type of types) {
    if (typeMap[type]) {
      return typeMap[type];
    }
  }

  return null;
};
```

### 初期値の設定（defaultValue）
```javascript
// Q1-1の初期値設定例
{
  id: 'Q1-1',
  defaultValue: (answers) => {
    const profile = answers['Q1-0-profile'];
    return profile?.businessType || '';
  },
  // ...
}
```
